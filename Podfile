# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LMiOSTemplate' do
  use_frameworks!

  # Pods for LMiOSTemplate
  pod 'Moya'

  target 'LMiOSTemplateTests' do
    inherit! :search_paths
    # Pods for testing
  end
end
